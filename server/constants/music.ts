export enum MusicAlbumStatus {
  UNKNOWN = 1,
  PENDING = 2,
  PROCESSING = 3,
  PARTIALLY_AVAILABLE = 4,
  AVAILABLE = 5,
}

export enum MusicAlbumType {
  ALBUM = 'album',
  SINGLE = 'single',
  EP = 'ep',
}
